tags:
  - workflow
  - permissions
category: workflow
targets:
  claude: true
  codex: true
  gemini: true
# Documentary global-config paths, exempt from portability checks. Other ~/ leaks
# on the same line still fail.
portability_allow:
  - "~/.codex/config.toml"
  - "~/.gemini/*.json"
