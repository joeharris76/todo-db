# Run Tests

Resolve named targets through config; use `single` for a path, `topic` for a
topic, and `fast` for an empty request. Summarize pass/fail counts, failure
patterns, slow tests, and the narrowest next step. For CI diagnosis, inspect
run/job status JSON before logs, fetch failed-job excerpts only, and run a
targeted local check before a full suite.
